{
  "name": "mcp-use",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "integrations/mcp-use",
  "tags": ["type:integration", "lang:javascript"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No build required for JavaScript package'",
        "cwd": "integrations/mcp-use"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'No tests configured yet for mcp-use'",
        "cwd": "integrations/mcp-use"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["integrations/mcp-use/**/*.js"]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish",
        "cwd": "integrations/mcp-use"
      }
    }
  }
}